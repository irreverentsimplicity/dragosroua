---
// Full-width black header
---

<header class="site-header">
  <div class="header-content">
    <div class="header-top">
      <img src="/images/dragos-hat.png" alt="Dragos Roua" class="header-image" />
      <div class="header-text">
        <h1><a href="/" aria-label="Dragos Roua - Go to homepage">Dragos Roua</a></h1>
        <p class="subtitle">Building addTaskManager, the best iOS productivity app for ADHD minds</p>
      </div>
    </div>
  </div>
  
  <nav class="main-nav">
    <div class="nav-content">
      <a href="/" aria-label="Go to homepage" aria-current={Astro.url.pathname === '/' ? 'page' : undefined}>Home</a>
      <a href="/blog/" aria-label="Read blog posts" aria-current={Astro.url.pathname.startsWith('/blog') || (!Astro.url.pathname.startsWith('/category') && !Astro.url.pathname.startsWith('/tag') && !['/', '/categories/', '/work-with-me/', '/advertise/', '/privacy-policy/', '/terms-of-service/', '/about-me/', '/books/', '/productivity-apps/'].includes(Astro.url.pathname)) ? 'page' : undefined}>Blog</a>
      <a href="/productivity-apps/" aria-label="Learn about addTaskManager app" aria-current={Astro.url.pathname.startsWith('/productivity-apps') ? 'page' : undefined}>App</a>
      <a href="/books/" aria-label="Browse books by Dragos Roua" aria-current={Astro.url.pathname.startsWith('/books') ? 'page' : undefined}>Books</a>
      <a href="/about-me/" aria-label="Learn about Dragos Roua" aria-current={Astro.url.pathname.startsWith('/about-me') ? 'page' : undefined}>About</a>
    </div>
  </nav>
</header>

<style>
  .site-header {
    background: #000000;
    color: white;
    width: 100%;
  }
  
  .header-content {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem 1rem 1.5rem;
  }
  
  .header-top {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }
  
  .header-image {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    object-fit: contain;
  }
  
  .header-text h1 {
    margin: 0;
    font-size: 2rem;
    font-weight: 700;
  }
  
  .header-text h1 a {
    color: #ffffff;
    text-decoration: none;
  }
  
  .subtitle {
    margin: 0.5rem 0 0;
    color: #cccccc;
    font-size: 1rem;
    line-height: 1.4;
  }
  
  .main-nav {
    background: #f8f9fa; /* Light gray background like footer */
    border-top: 1px solid #e9ecef;
    border-bottom: 1px solid #e9ecef;
  }
  
  .nav-content {
    max-width: 800px;
    margin: 0 auto;
    padding: 1rem 2rem;
    display: flex;
    justify-content: center;
    gap: 2rem;
  }
  
  .nav-content a {
    color: #0066cc !important; /* Hard blue like footer */
    text-decoration: none;
    font-weight: 500;
    transition: all 0.2s;
    padding: 0.25rem 0;
  }
  
  .nav-content a:hover {
    color: #0052a3 !important; /* Darker blue on hover */
    text-decoration: underline;
    text-decoration-thickness: 2px;
    text-underline-offset: 3px;
  }
  
  .nav-content a:focus {
    outline: 2px solid #0066cc;
    outline-offset: 2px;
    border-radius: 2px;
  }
  
  .nav-content a[aria-current="page"],
  .nav-content a.active {
    color: #0052a3 !important;
    text-decoration: underline;
    text-decoration-thickness: 2px;
    text-underline-offset: 3px;
    font-weight: 600;
  }
  
  @media (max-width: 640px) {
    .header-top {
      flex-direction: column;
      text-align: center;
    }
    
    .header-text h1 {
      font-size: 1.5rem;
    }
    
    .subtitle {
      font-size: 0.9rem;
    }
    
    .nav-content {
      gap: 1rem;
      flex-wrap: wrap;
      padding: 1rem;
    }
  }
</style>